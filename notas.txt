python -m venv venv (recomendado principalmente si se usan librerias respecificas o muy especialiadas)

venv\Scripts\activate

pip install flask
pip install flask_cors
pip install fastapi
pip install flask_sqlalchemy
pip install mysql-connector-python

python -m pip install -U black (FORMATEADOR)
